#!/usr/bin/python3
def uniq_add(my_list=[]):
    unique_integers = set(my_list) #create set to store unique integers
    sum_result = sum(unique_integers) #does math to unique integers
    return sum_result
